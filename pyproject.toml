[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "eyeloop"
version = "0.1.0"
description = "Python-based eye tracker for varifocal VR"
readme = "README.md"
requires-python = "==3.11.*"

[project.scripts]
eyeloop = "eyeloop.run_eyeloop:main"
